{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "env": {
    "PYTHONPATH": "./exchanges"
  },
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.11"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
